{
  "name": "chord-generator",
  "description": "Chord generator web app using a Rust-based WebAssembly module, just to play around.",
  "author": "Alex Emko",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:wasm": "wasm-pack build crates/wasm --target=web --out-dir=../../frontend/wasm"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  },
  "license": "ISC"
}
